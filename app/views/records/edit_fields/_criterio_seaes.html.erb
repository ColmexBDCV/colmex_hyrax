<%= f.input key,
            as: :select,
            collection: Qa::Authorities::Local.subauthority_for('criterio_seaes').all.map { |term| [term[:label], term[:id]] },
            input_html: { class: 'form-control', multiple: true },
            required: false %>
