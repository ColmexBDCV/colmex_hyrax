<% [:period, :part, :year, :volume, :number, :mode_of_issuance, :pages, :contained_in].each do |a| %>
    <%= presenter.attribute_to_html(a, label: t('hyrax.base.show.'+a.to_s), html_dl:true) %>
<% end %> 