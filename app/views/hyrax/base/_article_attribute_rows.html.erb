<% [:year, :volume, :number, :pages, :period, :part, :issn, :contained_in, :database, :is_part_or_work].each do |a| %>
    <%= presenter.attribute_to_html(a, label: t('hyrax.base.show.'+a.to_s), html_dl:true) %>
<% end %> 

