<% [:year, :volume, :number, :pages, :period, :part, :mode_of_issuance, :contained_in].each do |a| %>
    <%= presenter.attribute_to_html(a, label: t('hyrax.base.show.'+a.to_s), html_dl:true) %>
<% end %> 