
<% display_type = I18n.locale.to_s == 'es' ? 'colección' : 'collection' %>
<dt> <%= t("hyrax.base.relationships_parent_row.label", type: display_type) %> </dt>
<dd>
    <% if items.blank? %>
  <p><%= t('.empty', type: display_type) %></p>
    <% else %>
      <ul class="tabular">
        <% items.each do |item| %>
          <li class='attribute attribute-title'>
            <%= link_to item.title.first, url_for_document(item) %>
          </li>
        <% end %>
      </ul>
    <% end %>
  </dd>
